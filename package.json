{
  "name": "chat-server-lambda",
  "version": "1.1.0",
  "description": "Chat server on AWS Lambda.",
  "repository": "https://github.com/raravi/chat-server-lambda.git",
  "main": "server.js",
  "scripts": {
    "sls-offline": "sls offline start --skipCacheInvalidation --httpPort 9000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amith Raravi",
  "license": "MIT",
  "dependencies": {
    "login-server-express": "^1.1.11",
    "serverless-http": "^3.1.0"
  },
  "devDependencies": {
    "serverless-offline": "^11.2.3"
  }
}
